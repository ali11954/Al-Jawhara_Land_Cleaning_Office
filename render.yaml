services:
  - type: web
    name: al-jawhara-cleaning
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
envVars:
  - key: PYTHON_VERSION
    value: 3.11.9
